{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pictures list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>File</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pictures_show', { 'id': entity.id }) }}">{{ entity.file }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pictures_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pictures_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pictures_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

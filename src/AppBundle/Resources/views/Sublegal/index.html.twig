{% extends '::adminbase.html.twig' %}

{% block title %}
    Субподрядчики
{% endblock %}
{% block body -%}

    <div class="panel panel-default">
        <div class="panel-heading panel-header">
            <div class="header-left-block">
                <h3 class="panel-title">Список субподрядчиков</h3>
            </div>
            <div class="header-right-block">
                <a href="{{ path('sublegal_new') }}">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                    Добавить
                </a>
            </div>
        </div>
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th class="th-index th-class">#</th>
                    <th class="th-class">ФИО</th>
                    <th class="th-class">База</th>
                    <th class="th-action th-class">Действия</th>
                </tr>
            </thead>
            <tbody>
                {% for entity in entities %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td><a href="{{ path('sublegal_show', { 'id': entity.id }) }}">{{ entity.fio }}</a></td>
                        <td><a href="{{ path('sublegal_show', { 'id': entity.id }) }}">{{ entity.base.name }}</a></td>
                        <td>
                            <a href="{{ path('sublegal_edit', { 'id': entity.id }) }}">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                Изменить
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
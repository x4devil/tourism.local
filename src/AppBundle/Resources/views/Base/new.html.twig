{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Base creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('base') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
